# tmpfiles.d entries for net-porter
# Creates directory structure at early boot.
# Mode 0700: only root (server + worker) can access worker data.
d /run/net-porter 0700 root root -
d /run/net-porter/workers 0700 root root -
