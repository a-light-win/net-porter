# tmpfiles.d entry for net-porter
# Creates /run/net-porter at early boot for worker environment files.
# Mode 0700: only root (server + worker) accesses env files.
d /run/net-porter 0700 root root -
